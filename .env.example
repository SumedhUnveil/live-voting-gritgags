# Environment Variables for Live Voting App

# Backend URL - leave empty for auto-detection (recommended for local network)
# Set this only if deploying to a specific server
NEXT_PUBLIC_API_URL=

# Backend port (default: 3001)
PORT=3001
